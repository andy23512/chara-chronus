---
// Render target for the 1200x630 social preview image. It reuses HeaderPage so
// the image can never drift from the real hero. Regenerate with:
//   yarn og
import HeaderPage from '../components/HeaderPage';
import Layout from '../layouts/Layout.astro';
---

<Layout noindex showRibbon={false}>
	<HeaderPage/>
</Layout>
